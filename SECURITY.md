# SECURITY.md — Museland

> 這個 App 的資安地圖：架構、資料流、攻擊面分類，以及治理紀錄的位置。
>
> **這是公開文件。** 依資安治理母指令（`Individual_APP_Security_Review_Master_Prompt_V1.3.md`），公開 repository 只保存已遮罩、適合公開的中性摘要。**實際 findings、可利用細節、威脅建模與 release gate 狀態不在這份文件裡**，記錄於受控的 Security Register（見「治理紀錄位置」）。

## 1. 這個 App 是什麼

Museland（`https://museland.hsuanchao.com/`）是一個公開的個人娛樂／實驗站，以純靜態頁面為主，部署在 Vercel。訪客無帳號、無登入。多數設施自成一體、無伺服器依賴；其中感情模擬器這一項，依賴一支 serverless 端點代理呼叫外部 AI 服務。

## 2. 架構與資料流（中性描述）

### 2.1 靜態層（多數設施）
純 HTML/CSS/JS，無後端、無資料庫、無帳號。設施如認知貨幣、亂想劇場等，使用者互動在瀏覽器端處理。攻擊面屬一般靜態網站範疇（內容、標頭設定、第三方資源）。

### 2.2 動態層（感情模擬器）
單一 serverless 端點 `api/generate.js`，職責是接收前端送出的情境輸入、代理呼叫外部 AI 服務（Anthropic）生成場景並回傳。特性：

- **輸入來源**：公開、無需認證的前端表單。
- **對外呼叫**：以伺服器端持有的憑證呼叫第三方 AI API；憑證只存在 Vercel 環境變數，不在 repo、不進前端。
- **這是本 App 的主要資安關注點**：一個公開、無使用者認證、會產生第三方 API 費用的端點，其濫用防護（存取控制、限流、費用上限）與輸入處理是威脅建模的核心。

> 本端點的存取控制、限流與相關防護，其**設計、實作狀態與有效性**屬受控資訊，記錄於 Security Register，本檔不展開，也不在此陳述任何特定版本已修復或未修復。

### 2.3 第三方資料流揭露
使用感情模擬器時，使用者填入的情境內容**會傳送給第三方 AI 服務（Anthropic）**生成回應。使用者應避免填入可識別個人身分的真實資訊（真實姓名、聯絡方式等）。其餘設施不涉及第三方資料傳輸。

## 3. 攻擊面分類

| 面向 | 適用性 | 摘要 |
|---|---|---|
| 認證／授權 | 有限 | 無帳號系統；訪客匿名。 |
| 使用者輸入處理 | 適用 | `api/generate.js` 接收公開輸入並轉呼叫外部 AI；主要關注點。 |
| Secrets 管理 | 適用 | AI API 憑證等以環境變數保存，不在 repo。 |
| 費用／資源濫用 | 適用 | 公開 AI 代理端點的用量與費用防護。 |
| 第三方資料傳輸 | 適用 | 模擬器輸入傳送給 Anthropic；需向使用者揭露。 |
| 一般 Web 標頭／內容 | 適用 | 安全標頭與快取控制經由 `vercel.json`。 |
| 資料庫／持久化儲存 | 不適用 | 無資料庫、無伺服器端使用者資料儲存。 |
| 原生／行動 App | 不適用 | 純 Web。 |

## 4. 治理紀錄位置（Canonical Security Register）

本 App 的實際資安治理紀錄——findings、證據狀態、風險接受（RA）、release gate、修復與複測——保存於**受控的 Canonical App Security Register**，不在此公開 repo。可利用細節、修復步驟與 incident-sensitive 資訊一律留在受控層。

- **治理方法**：見 repo 內 `Individual_APP_Security_Review_Master_Prompt_V1.3.md`。
- **已知資安基線**：曾對上述動態端點進行 source-verified 的安全基線審查（識別代號 MUSE-SEC-001）。詳細 findings 與目前狀態記錄於受控 Register，不在此陳述。
- **部署與複測**：與該端點相關的修復部署動作與驗證步驟，記錄於受控的部署 runbook，不在此 repo。

## 5. 回報安全問題

如果你發現疑似安全問題，**請勿開公開 issue、勿公開揭露細節**，請透過私下管道聯繫維護者。這是個人專案，回應時間不保證；本制度目標是讓風險被識別、分級與處理，不等同正式第三方滲透測試或資安認證。

---

_本文件為公開、中性的資安地圖。任何可利用細節或 secret 一律不得寫入本檔或本 repo 其他公開檔案。_
